# Endpoint EDR Threat Detection Desk (kelly-endpoint-edr)

Monitor endpoint detection and response (EDR) malware alerts, memory injection, and host isolation commands.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

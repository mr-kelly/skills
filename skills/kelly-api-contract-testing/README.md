# OpenAPI Consumer Contract & Breaking Change Desk (kelly-api-contract-testing)

Validate microservice REST/gRPC API contracts, backward compatibility, and breaking schema changes.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

# Service Mesh Policy & Traffic Telemetry Desk (kelly-service-mesh)

Inspect service mesh mTLS policies, traffic split ratios, latency budgets, and microservice topology.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

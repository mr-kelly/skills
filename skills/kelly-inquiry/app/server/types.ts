// Domain types now live in lib/types.ts (shared by the data-provider layer,
// server, and scripts). Re-exported here so existing `./types.ts` imports keep
// working unchanged.
export * from "../../lib/types.ts";

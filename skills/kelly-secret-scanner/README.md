# Secret Scanner & Leakage Audit Desk (kelly-secret-scanner)

Audit repositories, config files, and logs for leaked secrets, API keys, and sensitive credentials.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

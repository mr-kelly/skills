# Database Sensitive Data Masking Desk (kelly-db-privacy-masking)

Enforce dynamic data masking and tokenization for PII/PHI in non-production database environments.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

{
  "name": "kelly-behavior-predict",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Predictive Recommendation Analytics Desk App-in-Skill — local dashboard over mock user-behavior funnel data with a deterministic, rule-based next-action heuristic (Hono server, zero-build vanilla frontend).",
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "start": "node app/server/launcher.ts",
    "serve": "node app/server/index.ts",
    "generate": "node scripts/generate_batch.ts",
    "validate": "node scripts/validate_ui_schema.ts"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "@hono/node-server": "^1.13.7"
  }
}

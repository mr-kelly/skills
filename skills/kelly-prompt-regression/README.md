# AI Prompt Regression & Cost Optimizer (kelly-prompt-regression)

Track prompt version regression tests, latency degradation, token cost efficiency, and fallback routing.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

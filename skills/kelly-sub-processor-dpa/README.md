# DPA & Vendor Sub-Processor Audit Desk (kelly-sub-processor-dpa)

Audit third-party vendor Data Processing Agreements (DPA), Standard Contractual Clauses (SCC), and sub-processors.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

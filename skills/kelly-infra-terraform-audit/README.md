# Terraform Infrastructure Drift & Security Desk (kelly-infra-terraform-audit)

Inspect Terraform / OpenTofu state drift, hardcoded security group risks, and cost impact previews.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

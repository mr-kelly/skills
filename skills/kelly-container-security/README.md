# Kubernetes Image Vulnerability Desk (kelly-container-security)

Audit container registry image vulnerabilities, CVE severity ratings, and Kyverno policy enforcement.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

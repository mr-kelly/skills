# API Security & Schema Violation Monitor (kelly-api-security-gateway)

Inspect API gateway traffic for OWASP API vulnerabilities, rate limit breaches, and credential stuffing.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

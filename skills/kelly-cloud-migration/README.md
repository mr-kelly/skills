# Multi-Cloud Workload Migration Analyzer (kelly-cloud-migration)

Assess on-prem and cloud workloads for multi-cloud migration complexity, licensing, and TCO.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

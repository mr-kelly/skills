# Corporate Expense & Receipt Audit Console (kelly-expense-audit)

Audit travel receipts, verify merchant authenticity, enforce expense policies, and flag out-of-policy claims.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

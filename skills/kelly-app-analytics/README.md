# App Product Analytics & Journey Desk (kelly-app-analytics)

Track product feature adoption, funnel conversion drop-offs, user retention cohorts, and feature usage.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

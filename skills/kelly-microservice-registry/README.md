# Microservice Service Catalog & Graph Desk (kelly-microservice-registry)

Maintain microservice ownership catalogs, API dependencies, tier-1 service SLOs, and drift alerts.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

# API Lifecycle & Schema Governance Console (kelly-api-governance)

Manage OpenAPI schemas, breaking change detection, rate limits, and API deprecation lifecycles.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

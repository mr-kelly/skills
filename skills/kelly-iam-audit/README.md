# IAM Privilege Audit & Identity Desk (kelly-iam-audit)

Audit user accounts, excess privileges, inactive credentials, and enforce least-privilege policies.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

# Kubernetes Pod Cost & Right-Sizing Desk (kelly-k8s-cost-allocator)

Allocate Kubernetes cluster CPU/Memory costs by namespace/team and generate right-sizing requests.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

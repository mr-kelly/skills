# Service Level Objective & Error Budget Desk (kelly-slo-monitor)

Track service SLOs, monitor error budget burn rates, and trigger freeze policies when budgets deplete.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

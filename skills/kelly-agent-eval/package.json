{
  "name": "kelly-agent-eval",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Agent Eval & Regression Board App-in-Skill — local review board comparing baseline vs candidate agent versions across a mock rubric-scored test suite (Hono server, zero-build vanilla frontend).",
  "engines": {
    "node": ">=23.6"
  },
  "scripts": {
    "start": "node app/server/launcher.ts",
    "serve": "node app/server/index.ts",
    "generate": "node scripts/generate_eval_run.ts",
    "validate": "node scripts/validate_ui_schema.ts",
    "export-report": "node scripts/export_release_report.ts"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "@hono/node-server": "^1.13.7"
  }
}

# Zero Trust Access & Device Trust Desk (kelly-zero-trust-access)

Manage zero-trust network access (ZTNA) policies, device health posture, and context-aware SSO authentication.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

# Zero-Downtime DB Schema Migration Desk (kelly-db-migration)

Plan backward-compatible database migrations, dual-write strategies, and lock-free DDL execution.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

# Cloud Security Posture & CSPM Inspector (kelly-cspm-checker)

Audit cloud security groups, public storage buckets, unencrypted assets, and compliance drift.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```

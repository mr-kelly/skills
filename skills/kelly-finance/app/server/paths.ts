export {
  appDir as APP_DIR,
  cacheDir as CACHE_DIR,
  dataDir as DATA_DIR,
  defaultHost as DEFAULT_HOST,
  defaultPort as DEFAULT_PORT,
  lockPath as LOCK_PATH,
  logPath as LOG_PATH,
  pidPath as PID_PATH,
  preferredPortMax as PREFERRED_PORT_MAX,
  serverDir as SERVER_DIR,
  skillDir as SKILL_DIR,
} from "../../lib/paths.ts";

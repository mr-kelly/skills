# Enterprise Knowledge RAG Ingestion Desk (kelly-knowledge-ingest)

Process unstructured enterprise documents, perform chunking optimization, and maintain vector search indexes.

## Usage

Run locally via canonical app project:
```bash
cd app
pnpm dev
```
